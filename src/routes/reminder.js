const router = require('express').Router()
const platform = require('../utils/platform.js');


/**
 * Authorize
 */
router.get('/bulk_messaging', async (req, res) => {
    console.log('');
})


module.exports = router;